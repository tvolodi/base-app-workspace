# Keycloak Authentication Configuration
# Configure these values to match your Keycloak server setup

# Keycloak Server URL
# Default: http://localhost:8080 (development)
# Production: https://your-keycloak-server.com
REACT_APP_KEYCLOAK_URL=http://localhost:8080

# Keycloak Realm Name
# This should match the realm created in your Keycloak admin console
REACT_APP_KEYCLOAK_REALM=base-app

# Keycloak Client ID
# This should match the client ID created in your Keycloak realm
REACT_APP_KEYCLOAK_CLIENT_ID=base-app-frontend

# API Backend URL (optional, for future use)
# REACT_APP_API_URL=http://localhost:8090

# Environment
# NODE_ENV=development

# Note: After changing these values, you need to rebuild the app:
# npm run build
